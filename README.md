Simple C++ Thread Pool
======================

Header-only, no dependency thread pool for parallel for loops implementation.

## Build and run tests

```bash
# prepare
mkdir build
cd build
cmake ..

# build
make

# run test
./test_threadpool
```
